<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Hexapod Dashboard - Wireframe Only</title>
  <style>
    :root{
      --border:#2b2b2b;
      --panel-bg:#ffffff;
      --text:#222;
      --muted:#555;
      --gap:18px;
      --radius:8px;
    }

    *{ box-sizing:border-box; }
    body{
      margin:0;
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      background:#fff;   /* plain background */
      color:var(--text);
    }

    .wrap{
      width:min(1100px, 92vw);
      margin: 28px auto;
    }

    .grid{
      display:grid;
      grid-template-columns: 2.2fr 1fr;
      grid-template-rows: 320px 240px auto;
      gap: var(--gap);
    }

    .panel{
      background: var(--panel-bg);
      border: 2px solid var(--border);
      border-radius: var(--radius);
      padding: 14px;
      display:flex;
      align-items:center;
      justify-content:center;
      text-align:center;
      line-height:1.35;
      color:var(--muted);
      font-size: 14px;
      min-height: 80px;
    }

    /* Layout to match your wireframe */
    .camera { grid-column: 1; grid-row: 1 / span 2; }
    .gas    { grid-column: 2; grid-row: 1; }
    .sensors{ grid-column: 2; grid-row: 2; }
    .footer { grid-column: 1 / span 2; grid-row: 3; }

    /* Mobile: stack everything */
    @media (max-width: 900px){
      .grid{
        grid-template-columns: 1fr;
        grid-template-rows: auto;
      }
      .camera,.gas,.sensors,.footer{
        grid-column: 1;
        grid-row:auto;
      }
      .camera{ min-height: 380px; }
    }
  </style>
</head>

<body>
  <div class="wrap">
    <div class="grid">
      <div class="panel camera">Live Camera Feed</div>

      <div class="panel gas">Gas Graph</div>

      <div class="panel sensors">
        Other sensor information<br/>
        <span style="font-size:12px;">
          (Can adjust amount of blocks depending on sensors)
        </span>
      </div>

      <div class="panel footer">
        Additional Information (Battery Level, Basic Controls Info, etc)
      </div>
    </div>
  </div>
</body>
</html>
